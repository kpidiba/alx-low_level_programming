#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always success
 */

int main(void)
{
	int z;

	for (z = 'z' ; z >= 'a'; z--)
		putchar(z);
	putchar('\n');
return (0);

}
